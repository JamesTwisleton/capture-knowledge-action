# Dependencies
node_modules/

# Build outputs
target/
.next/
out/
build/
dist/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
**/.DS_Store

# Logs
*.log

# Local configuration and secrets.
# .env is generated from .env.example (and, from T20, by ./setup.sh) and holds real
# credentials — Google OAuth secret, GitHub token, LLM and Jev keys. Never commit it.
.env
.env.local
.env.*.local

# Next.js generates this; it is not source.
next-env.d.ts
*.tsbuildinfo

# Runtime data: the knowledge vault (T06) and SQLite database (T10). Yours, not the repo's.
data/
